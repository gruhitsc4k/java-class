import java.util.ArrayList;

class Main{
    /*
    Create a binarySearch()
    */

    public static void main(String args[]){
        /*
            ArrayList<DataType> varname = new ArrayList<DataType>();
        */

        int arr[] = new int[3];

        ArrayList<Integer> arr1 = new ArrayList<Integer>();

        // varname.add(element)
        arr1.add(10);
        arr1.add(20);

        for(int i = 0; arr1.size(); i++){
            System.out.println("Element[" + i + "] :- " + arr1.get(i) );
        }

        // varname.add(index, element);
    }
}